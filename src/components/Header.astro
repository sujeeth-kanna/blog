---
const links = [
  { href: '/projects', label: 'projects' },
  { href: '/notes', label: 'notes' },
  { href: '/about', label: 'about' },
];
---

<header class="site-header">
  <a href="/" class="brand">~/sujeeth</a>
  <nav class="site-nav">
    {links.map((l) => <a href={l.href}>{l.label}</a>)}
  </nav>
</header>
